{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel watch --public-url /static/ --out-dir ../static $(find entry-points -type f)",
    "build": "parcel build --public-url /static/ --out-dir ../static $(find entry-points -type f)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vue/component-compiler-utils": "^3.1.1",
    "parcel": "^1.12.4",
    "sass": "^1.26.8",
    "typescript": "^3.7.5",
    "vue-template-compiler": "^2.6.11"
  },
  "dependencies": {
    "@ericblade/quagga2": "1.7.4",
    "vue": "^2.6.11",
    "vue-hot-reload-api": "^2.3.4"
  }
}